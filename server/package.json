{
	"name": "app",
	"version": "1.0.50",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "bun run --watch src/index.ts",
		"lint": "bun prettier . --check --config ../prettier.config.mjs",
		"lint-write": "bun prettier . --write --config ../prettier.config.mjs"
	},
	"dependencies": {
		"elysia": "latest"
	},
	"devDependencies": {
		"bun-types": "latest",
		"prettier": "3.3.3"
	},
	"module": "src/index.ts"
}
